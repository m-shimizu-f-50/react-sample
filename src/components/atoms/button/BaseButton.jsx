import styled from 'styled-components';

export const BaseButton = styled.button`
	color: white;
	border: none;
	padding: 10px 20px;
	border-radius: 5px;
	cursor: pointer;
	font-size: 16px;

	&:hover {
		cursor: pointer;
		opacity: 0.8;
	}
`;
